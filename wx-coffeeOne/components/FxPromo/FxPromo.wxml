<!-- 活动页面，大的 -->
<view class="fx-promo" data-rid="{{ product.rid }}" data-title="{{ product.name }}" data-type="{{ product.type }}" bindtap="viewTap">
  <view class="fx-promo__image">
    <!-- <image src="{{ product.cover }}" mode="widthFix" class="cover" ></image> -->
    <image src="{{ product.cover }}" class="cover">
    </image>
    
    <view class="fx-promo__title">
      活动
    </view>
  </view>
</view>